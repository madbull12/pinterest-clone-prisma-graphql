### This file was generated by Nexus Schema
### Do not make changes to this file directly


type Board {
  id: String
  name: String
  saved: [Saved]
  secret: Boolean
  user: User
  userId: String
}

type Comment {
  content: String
  createdAt: DateTime
  id: String
  pin: Pin
  pinId: String
  user: User
  userId: String
}

"""
A date-time string at UTC, such as 2007-12-03T10:15:30Z, compliant with the `date-time` format outlined in section 5.6 of the RFC 3339 profile of the ISO 8601 standard for representation of dates and times using the Gregorian calendar.
"""
scalar DateTime

type Mutation {
  createBoard(name: String!, secret: Boolean!, userId: String!): Board!
  createComment(content: String!, pinId: String!, userId: String!): Comment!
  createPin(category: [String], description: String, imageUrl: String!, title: String!, userId: String!): Pin!
  deleteSave(saveId: String!): Saved!
  savePin(boardId: String!, pinId: String!, userId: String!): Saved!
}

type Pin {
  category: [String]
  comments: [Comment]
  createdAt: DateTime
  description: String
  id: String
  imageUrl: String
  title: String
  user: User
  userId: String
}

type Query {
  BoardPins: Board!
  FirstUserBoard(userId: String!): Board!
  UserBoards(userId: String!): [Board!]
  pin(pinId: String!): Pin!
  pins: [Pin]!
  saved: [Saved]!
  searchPins(searchTerm: String!): [Pin]!
  user(userId: String!): User!
  userSaved(userId: String!): [Saved]!
}

enum Role {
  ADMIN
  USER
}

type Saved {
  createdAt: DateTime
  id: String
  pin: Pin
  pinId: String
  user: User
  userId: String
}

type User {
  email: String
  id: String
  image: String
  pins: [Pin]
  role: Role
}